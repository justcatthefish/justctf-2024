# not-so-easy task utilizing the DenyList feature

admin, or the contract's function will transfer a `Coin<T>` object to the user, and blacklist the immediately. the main objective will be to transfer this `Coin<T>` to other address.
